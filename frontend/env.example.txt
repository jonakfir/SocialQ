# Rename to .env (or copy these lines into .env)
# SvelteKit: PUBLIC_* variables are available at build and in server routes

PUBLIC_API_URL=https://socialq-production.up.railway.app

# Required for admin/organizations and other Prisma-backed features (PostgreSQL).
# Get a URL from Railway, Supabase, Neon, or run PostgreSQL locally.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
# Example (local): DATABASE_URL=postgresql://postgres:postgres@localhost:5432/socialq
DATABASE_URL=

# Optional: same secret as backend PROXY_SECRET; required for backend to proxy friend photos and ekman collages
# See SETUP_FRIEND_PHOTOS_ENV.md in web/ for step-by-step.
# BACKEND_PROXY_SECRET=your-shared-secret
