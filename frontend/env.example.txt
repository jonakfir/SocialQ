# Rename to .env (or copy these lines into .env)
# SvelteKit: PUBLIC_* variables are available at build and in server routes

PUBLIC_API_URL=https://socialq-production.up.railway.app

# Required for admin/organizations and other Prisma-backed features (PostgreSQL).
# Get a URL from Railway, Supabase, Neon, or run PostgreSQL locally.
# Required for Vercel (admin panel, /api/*). Use Railway Postgres PUBLIC URL.
# In Railway: Postgres service → Variables → copy DATABASE_PUBLIC_URL.
# Example: postgresql://postgres:PASSWORD@ballast.proxy.rlwy.net:25477/railway
# See VERCEL_DATABASE.md for full steps.
DATABASE_URL=

# Same as DATABASE_URL (required for Prisma). Used for transactions so collage upload FK works with pooled URLs.
# On Vercel: set DIRECT_URL to the same value as DATABASE_URL.
DIRECT_URL=

# When set, POST /api/collages is proxied to this URL (your Railway backend). Backend saves to its DB = no FK issue.
# Example: https://your-app.up.railway.app
# BACKEND_COLLAGES_URL=

# Optional: same secret as backend PROXY_SECRET; required for backend to proxy friend photos and ekman collages
# See SETUP_FRIEND_PHOTOS_ENV.md in web/ for step-by-step.
# BACKEND_PROXY_SECRET=your-shared-secret
