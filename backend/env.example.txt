# Copy these lines into your .env file (do not commit .env with real secrets)

# Backend environment variables for Railway/production
PUBLIC_API_URL=https://your-app.up.railway.app
PRIVATE_API_BASE=https://your-app.up.railway.app
DATABASE_URL=postgresql://user:password@host:port/database

# Optional: proxy to frontend for friend photos, recognition, and collage upload (iOS when API_BASE_URL points here)
# Required for iOS "Approve Anyway" / collage upload + friend's photos to work when the app uses the backend URL
# See SETUP_FRIEND_PHOTOS_ENV.md in web/ for step-by-step.
# FRONTEND_URL=https://social-q-theta.vercel.app
# PROXY_SECRET=your-shared-secret  (same value as BACKEND_PROXY_SECRET on Vercel)

# Optional: email when a new user signs up (NOTIFY_EMAIL = your email; SMTP_* = your mailer)
# NOTIFY_EMAIL=you@example.com
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# Stripe (payments / access levels)
STRIPE_SECRET_KEY=sk_live_... or sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_live_... or pk_test_...
# After creating a webhook in Stripe Dashboard → Developers → Webhooks, paste the signing secret:
STRIPE_WEBHOOK_SECRET=whsec_...
# Price IDs from Stripe Dashboard → Product catalog (create products/prices, then paste IDs here):
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_FREE_TRIAL=price_...
